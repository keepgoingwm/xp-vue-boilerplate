/**
 * 基础
 * ----------------------------------------------------------------------------
 */
export const global = ({ global }) => {
  return global
}
export const setting = ({ setting }) => {
  return setting
}
